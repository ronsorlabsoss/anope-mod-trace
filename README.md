# TRACE and ALERT for Anope Services

Install steps:

1. make import ANOPE=/path/to/anope/source
2. Re-run CMake in Anope source dir
3. make install
4. add the contents of `trace.conf` to `operserv.conf`


